/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {
    domains: ['d3cvnrw4bpahxk.cloudfront.net'],
  },

}

module.exports = nextConfig
